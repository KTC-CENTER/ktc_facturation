{% extends 'base.html.twig' %}

{% block title %}Modifier {{ invoice.reference }}{% endblock %}

{% block content %}
<div class="space-y-6">
    <div>
        <nav class="text-sm text-gray-500 mb-2">
            <a href="{{ path('app_invoice_index') }}" class="hover:text-primary-600">Factures</a>
            <span class="mx-2">/</span>
            <a href="{{ path('app_invoice_show', {id: invoice.id}) }}" class="hover:text-primary-600">{{ invoice.reference }}</a>
            <span class="mx-2">/</span>
            <span class="text-gray-700">Modifier</span>
        </nav>
        <h1 class="text-2xl font-bold text-gray-900">Modifier la facture</h1>
    </div>

    {{ include('invoice/_form.html.twig', {button_label: 'Enregistrer les modifications'}) }}
</div>
{% endblock %}
