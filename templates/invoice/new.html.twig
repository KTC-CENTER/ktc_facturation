{% extends 'base.html.twig' %}

{% block title %}Nouvelle facture{% endblock %}

{% block content %}
<div class="space-y-6">
    <div>
        <nav class="text-sm text-gray-500 mb-2">
            <a href="{{ path('app_invoice_index') }}" class="hover:text-primary-600">Factures</a>
            <span class="mx-2">/</span>
            <span class="text-gray-700">Nouvelle</span>
        </nav>
        <h1 class="text-2xl font-bold text-gray-900">Nouvelle facture</h1>
    </div>

    {{ include('invoice/_form.html.twig', {button_label: 'Créer la facture'}) }}
</div>
{% endblock %}
