<!DOCTYPE html>
<html lang="fr" class="h-full bg-gray-50">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}KTC Invoice Pro{% endblock %}</title>
    
    <!-- Favicon -->
    {% if company_favicon is defined and company_favicon %}
    <link rel="icon" href="{{ asset('uploads/logo/' ~ company_favicon) }}">
    {% else %}
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect width='100' height='100' rx='20' fill='%231E3A5F'/><text x='50' y='68' font-size='50' font-weight='bold' text-anchor='middle' fill='white' font-family='system-ui'>K</text></svg>">
    {% endif %}

    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* FOUC protection: prevent flash of unstyled content while Tailwind CDN processes */
        body { opacity: 0; transition: opacity 0.15s ease-in; }
        body.ready { opacity: 1; }
    </style>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#f0f9ff', 100: '#e0f2fe', 200: '#bae6fd',
                            300: '#7dd3fc', 400: '#38bdf8', 500: '#0ea5e9',
                            600: '#0284c7', 700: '#0369a1', 800: '#075985', 900: '#0c4a6e',
                        },
                        ktc: { dark: '#1E3A5F', blue: '#2E86AB', light: '#A3C4DC' },
                        success: {
                            50: '#ecfdf5', 100: '#d1fae5', 200: '#a7f3d0', 300: '#6ee7b7',
                            400: '#34d399', 500: '#10b981', 600: '#059669', 700: '#047857',
                            800: '#065f46', 900: '#064e3b',
                        },
                        danger: {
                            50: '#fef2f2', 100: '#fee2e2', 200: '#fecaca', 300: '#fca5a5',
                            400: '#f87171', 500: '#ef4444', 600: '#dc2626', 700: '#b91c1c',
                            800: '#991b1b', 900: '#7f1d1d',
                        },
                        warning: {
                            50: '#fffbeb', 100: '#fef3c7', 200: '#fde68a', 300: '#fcd34d',
                            400: '#fbbf24', 500: '#f59e0b', 600: '#d97706', 700: '#b45309',
                            800: '#92400e', 900: '#78350f',
                        },
                        info: {
                            50: '#eff6ff', 100: '#dbeafe', 200: '#bfdbfe', 300: '#93c5fd',
                            400: '#60a5fa', 500: '#3b82f6', 600: '#2563eb', 700: '#1d4ed8',
                            800: '#1e40af', 900: '#1e3a8a',
                        }
                    }
                }
            }
        }
    </script>
    
    <!-- Alpine.js -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.8/dist/cdn.min.js"></script>
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- Select2 -->
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    
    <style>
        [x-cloak] { display: none !important; }

        /* ===== BOUTONS ===== */
        .btn {
            display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
            padding: 0.5rem 1rem; font-size: 0.875rem; line-height: 1.25rem; font-weight: 500;
            border-radius: 0.5rem; transition: all 200ms;
            text-decoration: none; cursor: pointer; border: none;
        }
        .btn:focus { outline: none; box-shadow: 0 0 0 2px #fff, 0 0 0 4px #93c5fd; }
        .btn:disabled { opacity: 0.5; cursor: not-allowed; }
        .btn-primary {
            display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
            padding: 0.5rem 1rem; font-size: 0.875rem; font-weight: 500; border-radius: 0.5rem;
            background-color: #1E3A5F; color: #fff; transition: all 200ms;
            text-decoration: none; cursor: pointer; border: none;
        }
        .btn-primary:hover { background-color: #2E86AB; color: #fff; }
        .btn-primary:focus { outline: none; box-shadow: 0 0 0 2px #fff, 0 0 0 4px #2E86AB; }
        .btn-secondary {
            display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
            padding: 0.5rem 1rem; font-size: 0.875rem; font-weight: 500; border-radius: 0.5rem;
            background-color: #fff; color: #374151; border: 1px solid #d1d5db; transition: all 200ms;
            text-decoration: none; cursor: pointer;
        }
        .btn-secondary:hover { background-color: #f9fafb; }
        .btn-secondary:focus { outline: none; box-shadow: 0 0 0 2px #fff, 0 0 0 4px #9ca3af; }
        .btn-success {
            display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
            padding: 0.5rem 1rem; font-size: 0.875rem; font-weight: 500; border-radius: 0.5rem;
            background-color: #059669; color: #fff; transition: all 200ms;
            text-decoration: none; cursor: pointer; border: none;
        }
        .btn-success:hover { background-color: #047857; }
        .btn-danger {
            display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
            padding: 0.5rem 1rem; font-size: 0.875rem; font-weight: 500; border-radius: 0.5rem;
            background-color: #dc2626; color: #fff; transition: all 200ms;
            text-decoration: none; cursor: pointer; border: none;
        }
        .btn-danger:hover { background-color: #b91c1c; }
        .btn-warning {
            display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
            padding: 0.5rem 1rem; font-size: 0.875rem; font-weight: 500; border-radius: 0.5rem;
            background-color: #f59e0b; color: #fff; transition: all 200ms;
            text-decoration: none; cursor: pointer; border: none;
        }
        .btn-warning:hover { background-color: #d97706; }
        .btn-ghost {
            display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
            padding: 0.5rem 1rem; font-size: 0.875rem; font-weight: 500; border-radius: 0.5rem;
            color: #4b5563; transition: all 200ms; background: transparent;
            text-decoration: none; cursor: pointer; border: none;
        }
        .btn-ghost:hover { background-color: #f3f4f6; }
        .btn-sm { padding: 0.375rem 0.75rem; font-size: 0.75rem; }
        .btn-lg { padding: 0.75rem 1.5rem; font-size: 1rem; }
        .btn-icon { padding: 0.5rem; }

        /* ===== CARDS ===== */
        .card {
            background-color: #fff; border-radius: 0.75rem; padding: 1.5rem;
            box-shadow: 0 1px 3px 0 rgba(0,0,0,.1), 0 1px 2px -1px rgba(0,0,0,.1);
            border: 1px solid #e5e7eb;
        }
        .card-header {
            padding: 1rem 1.5rem; border-bottom: 1px solid #e5e7eb;
            margin: -1.5rem -1.5rem 1.5rem -1.5rem; border-radius: 0.75rem 0.75rem 0 0;
        }
        .card-body { padding: 0; }
        .card-footer {
            padding: 1rem 1.5rem; border-top: 1px solid #e5e7eb;
            margin: 1.5rem -1.5rem -1.5rem -1.5rem; border-radius: 0 0 0.75rem 0.75rem;
            background-color: #f9fafb;
        }

        /* ===== FORMULAIRES ===== */
        .form-group { display: flex; flex-direction: column; gap: 0.375rem; }
        .form-label { display: block; font-size: 0.875rem; font-weight: 500; color: #374151; }
        .form-input {
            display: block; width: 100%; padding: 0.5rem 0.75rem; font-size: 0.875rem;
            border: 1px solid #d1d5db; border-radius: 0.5rem; background-color: #fff;
            transition: border-color 150ms, box-shadow 150ms;
            box-sizing: border-box;
        }
        .form-input::placeholder { color: #9ca3af; }
        .form-input:focus { border-color: #2E86AB; box-shadow: 0 0 0 1px #2E86AB; outline: none; }
        .form-input:disabled { background-color: #f3f4f6; color: #6b7280; }
        .form-select {
            display: block; width: 100%; padding: 0.5rem 2.5rem 0.5rem 0.75rem; font-size: 0.875rem;
            border: 1px solid #d1d5db; border-radius: 0.5rem; background-color: #fff;
            appearance: none; background-repeat: no-repeat;
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e");
            background-position: right 0.5rem center; background-size: 1.25rem;
            box-sizing: border-box;
        }
        .form-select:focus { border-color: #2E86AB; box-shadow: 0 0 0 1px #2E86AB; outline: none; }
        .form-textarea {
            display: block; width: 100%; padding: 0.5rem 0.75rem; font-size: 0.875rem;
            border: 1px solid #d1d5db; border-radius: 0.5rem; background-color: #fff;
            resize: vertical; min-height: 80px; box-sizing: border-box;
        }
        .form-textarea::placeholder { color: #9ca3af; }
        .form-textarea:focus { border-color: #2E86AB; box-shadow: 0 0 0 1px #2E86AB; outline: none; }
        .form-checkbox {
            height: 1rem; width: 1rem; color: #2E86AB; border: 1px solid #d1d5db;
            border-radius: 0.25rem;
        }
        .form-checkbox:focus { box-shadow: 0 0 0 2px #2E86AB; }
        .form-error { font-size: 0.75rem; color: #dc2626; margin-top: 0.25rem; }
        .form-hint { font-size: 0.75rem; color: #6b7280; margin-top: 0.25rem; }

        /* ===== TABLES ===== */
        .table-container { overflow-x: auto; border-radius: 0.5rem; border: 1px solid #e5e7eb; }
        .table { min-width: 100%; border-collapse: separate; border-spacing: 0; }
        .table th {
            padding: 0.75rem 1rem; text-align: left; font-size: 0.75rem; font-weight: 600;
            color: #4b5563; text-transform: uppercase; letter-spacing: 0.05em; background-color: #f9fafb;
        }
        .table td { padding: 0.75rem 1rem; font-size: 0.875rem; color: #374151; }
        .table tbody tr { transition: background-color 150ms; border-bottom: 1px solid #f3f4f6; }
        .table tbody tr:hover { background-color: #f9fafb; }

        /* ===== BADGES ===== */
        .badge {
            display: inline-flex; align-items: center; padding: 0.125rem 0.625rem;
            border-radius: 9999px; font-size: 0.75rem; font-weight: 500;
        }
        .badge-gray { background-color: #f3f4f6; color: #374151; }
        .badge-blue { background-color: #dbeafe; color: #1d4ed8; }
        .badge-green, .badge-success { background-color: #d1fae5; color: #065f46; }
        .badge-yellow, .badge-warning { background-color: #fef3c7; color: #92400e; }
        .badge-red, .badge-danger { background-color: #fee2e2; color: #991b1b; }
        .badge-primary { background-color: #dbeafe; color: #1e40af; }
        .badge-info { background-color: #e0f2fe; color: #0369a1; }
        .badge-secondary { background-color: #f3f4f6; color: #4b5563; }
        .badge-lg { padding: 0.25rem 0.75rem; font-size: 0.875rem; }

        /* ===== ALERTS ===== */
        .alert { padding: 1rem; border-radius: 0.5rem; display: flex; gap: 0.75rem; font-size: 0.875rem; }
        .alert-success { background-color: #ecfdf5; color: #065f46; }
        .alert-danger { background-color: #fef2f2; color: #991b1b; }
        .alert-warning { background-color: #fffbeb; color: #92400e; }
        .alert-info { background-color: #eff6ff; color: #1e40af; }

        /* ===== PAGINATION ===== */
        .pagination { display: flex; align-items: center; justify-content: center; gap: 0.25rem; padding: 0.75rem 0; }
        .pagination a, .pagination span {
            padding: 0.375rem 0.75rem; font-size: 0.875rem; font-weight: 500;
            border-radius: 0.5rem; transition: all 150ms; text-decoration: none;
        }
        .pagination a { color: #4b5563; }
        .pagination a:hover { background-color: #f3f4f6; }
        .pagination .current { background-color: #1E3A5F; color: #fff; }
        .pagination .disabled { color: #9ca3af; cursor: not-allowed; }

        /* ===== STAT CARDS ===== */
        .stat-card {
            background-color: #fff; border-radius: 0.75rem; padding: 1.5rem;
            box-shadow: 0 1px 3px 0 rgba(0,0,0,.1), 0 1px 2px -1px rgba(0,0,0,.1);
            border: 1px solid #e5e7eb;
        }
        .stat-value { font-size: 1.5rem; font-weight: 700; color: #111827; }
        .stat-label { font-size: 0.875rem; color: #6b7280; margin-top: 0.25rem; }

        /* ===== SELECT2 ===== */
        .select2-container--default .select2-selection--single {
            height: 38px !important; padding: 4px 12px !important;
            border-radius: 0.5rem !important; border-color: #d1d5db !important;
        }
        .select2-container--default .select2-selection--single .select2-selection__rendered { line-height: 28px !important; }
        .select2-container--default .select2-selection--single .select2-selection__arrow { height: 36px !important; }

        /* ===== UTILITIES ===== */
        .line-clamp-1 { overflow: hidden; display: -webkit-box; -webkit-line-clamp: 1; -webkit-box-orient: vertical; }
        .inline { display: inline; }

        /* ===== PRINT ===== */
        @media print {
            .no-print { display: none !important; }
            .print-only { display: block !important; }
        }
    </style>
    {% block stylesheets %}{% endblock %}
            {{ encore_entry_link_tags('app') }}
</head>
<body class="h-full" x-data="{ sidebarOpen: false }">
    {% if app.user %}
    <div class="min-h-full">
        <!-- Sidebar mobile overlay -->
        <div x-show="sidebarOpen" class="relative z-50 lg:hidden" x-cloak>
            <div x-show="sidebarOpen" 
                 x-transition:enter="transition-opacity ease-linear duration-300"
                 x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100"
                 x-transition:leave="transition-opacity ease-linear duration-300"
                 x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0"
                 class="fixed inset-0 bg-gray-900/80" @click="sidebarOpen = false"></div>
            <div class="fixed inset-0 flex">
                <div x-show="sidebarOpen" 
                     x-transition:enter="transition ease-in-out duration-300 transform"
                     x-transition:enter-start="-translate-x-full" x-transition:enter-end="translate-x-0"
                     x-transition:leave="transition ease-in-out duration-300 transform"
                     x-transition:leave-start="translate-x-0" x-transition:leave-end="-translate-x-full"
                     class="relative mr-16 flex w-full max-w-xs flex-1">
                    {% include 'layout/_sidebar.html.twig' %}
                </div>
            </div>
        </div>

        <!-- Sidebar desktop -->
        <div class="hidden lg:fixed lg:inset-y-0 lg:z-50 lg:flex lg:w-64 lg:flex-col">
            {% include 'layout/_sidebar.html.twig' %}
        </div>

        <div class="lg:pl-64">
            <!-- Top bar -->
            <div class="sticky top-0 z-40 flex h-16 shrink-0 items-center gap-x-4 border-b border-gray-200 bg-white px-4 shadow-sm sm:gap-x-6 sm:px-6 lg:px-8 no-print">
                <button type="button" class="-m-2.5 p-2.5 text-gray-700 lg:hidden" @click="sidebarOpen = true">
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                    </svg>
                </button>
                <div class="h-6 w-px bg-gray-200 lg:hidden"></div>
                <div class="flex flex-1 gap-x-4 self-stretch lg:gap-x-6">
                    <div class="flex flex-1 items-center">
                        <h1 class="text-lg font-semibold text-gray-900">{% block page_title %}{% endblock %}</h1>
                    </div>
                    <div class="flex items-center gap-x-4 lg:gap-x-6">
                        <div x-data="{ open: false }" class="relative">
                            <button @click="open = !open" class="-m-1.5 flex items-center p-1.5">
                                <span class="inline-flex h-8 w-8 items-center justify-center rounded-full bg-ktc-dark">
                                    <span class="text-sm font-medium text-white">
                                        {{ app.user.firstName|first|upper }}{{ app.user.lastName|first|upper }}
                                    </span>
                                </span>
                                <span class="hidden lg:flex lg:items-center">
                                    <span class="ml-3 text-sm font-semibold text-gray-900">{{ app.user.fullName }}</span>
                                    <svg class="ml-2 h-5 w-5 text-gray-400" viewBox="0 0 20 20" fill="currentColor">
                                        <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
                                    </svg>
                                </span>
                            </button>
                            <div x-show="open" @click.away="open = false" x-cloak
                                 x-transition:enter="transition ease-out duration-100"
                                 x-transition:enter-start="opacity-0 scale-95" x-transition:enter-end="opacity-100 scale-100"
                                 x-transition:leave="transition ease-in duration-75"
                                 x-transition:leave-start="opacity-100 scale-100" x-transition:leave-end="opacity-0 scale-95"
                                 class="absolute right-0 z-10 mt-2.5 w-48 origin-top-right rounded-md bg-white py-2 shadow-lg ring-1 ring-gray-900/5">
                                <a href="{{ path('app_profile') }}" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50">
                                    <svg class="inline w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
                                    Mon profil
                                </a>
                                {% if is_granted('ROLE_ADMIN') %}
                                <a href="{{ path('app_settings_index') }}" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50">
                                    <svg class="inline w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                                    Paramètres
                                </a>
                                {% endif %}
                                <hr class="my-1 border-gray-100">
                                <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-sm text-red-600 hover:bg-red-50">
                                    <svg class="inline w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
                                    Déconnexion
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <main class="py-6">
                <div class="px-4 sm:px-6 lg:px-8">
                    <!-- Flash messages -->
                    {% for type, messages in app.flashes %}
                        {% for message in messages %}
                        <div class="mb-4 rounded-lg p-4 {{ type == 'success' ? 'bg-emerald-50 text-emerald-800' : (type == 'error' ? 'bg-red-50 text-red-800' : (type == 'warning' ? 'bg-amber-50 text-amber-800' : 'bg-blue-50 text-blue-800')) }}"
                             x-data="{ show: true }" x-show="show" x-init="setTimeout(() => show = false, 5000)">
                            <div class="flex items-center justify-between">
                                <p class="text-sm font-medium">{{ message }}</p>
                                <button @click="show = false" class="ml-4 opacity-70 hover:opacity-100">&times;</button>
                            </div>
                        </div>
                        {% endfor %}
                    {% endfor %}

                    {% block content %}{% endblock %}
                </div>
            </main>
        </div>
    </div>
    {% else %}
        {% block body %}{% endblock %}
    {% endif %}

    <!-- jQuery & Select2 -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script>
        $(document).ready(function() {
            $('.select2').select2({ placeholder: 'Sélectionner...', allowClear: true });
        });
    </script>
    {% block javascripts %}{% endblock %}
            {{ encore_entry_script_tags('app') }}
    <script>document.body.classList.add('ready');</script>
</body>
</html>
