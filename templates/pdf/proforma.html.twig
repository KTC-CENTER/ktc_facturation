<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Proforma {{ proforma.reference }}</title>
    <style>
        @page {
            margin: 8mm 10mm 15mm 10mm;
            size: A4;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'DejaVu Sans', Helvetica, Arial, sans-serif;
            font-size: 9pt;
            color: #1a1a1a;
            line-height: 1.35;
            background: #fff;
        }

        /* ==================== HEADER ==================== */
        .header {
            display: table;
            width: 100%;
            margin-bottom: 12px;
            border-bottom: 3px solid #0066CC;
            padding-bottom: 10px;
        }
        
        .header-left {
            display: table-cell;
            width: 50%;
            vertical-align: middle;
        }
        
        .header-right {
            display: table-cell;
            width: 50%;
            vertical-align: middle;
            text-align: right;
        }
        
        .logo-container {
            display: inline-block;
        }
        
        .logo {
            max-height: 55px;
            max-width: 160px;
        }
        
        .company-text-logo {
            font-size: 22pt;
            font-weight: bold;
            color: #0066CC;
            letter-spacing: -0.5px;
        }
        
        .company-text-logo span {
            color: #FF6600;
        }
        
        .slogan {
            font-size: 7.5pt;
            color: #0066CC;
            font-style: italic;
            margin-top: 2px;
        }
        
        .doc-type {
            font-size: 20pt;
            font-weight: bold;
            color: #CC0000;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .doc-meta {
            margin-top: 6px;
            font-size: 8.5pt;
            color: #444;
        }
        
        .doc-meta-row {
            margin: 2px 0;
        }
        
        .doc-meta-label {
            color: #666;
        }
        
        .doc-meta-value {
            font-weight: 600;
            color: #1a1a1a;
        }

        /* ==================== PARTIES ==================== */
        .parties {
            display: table;
            width: 100%;
            margin-bottom: 15px;
        }
        
        .party-emetteur {
            display: table-cell;
            width: 48%;
            vertical-align: top;
        }
        
        .party-spacer {
            display: table-cell;
            width: 4%;
        }
        
        .party-client {
            display: table-cell;
            width: 48%;
            vertical-align: top;
        }
        
        .party-label {
            font-size: 8pt;
            color: #666;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 6px;
            font-weight: 600;
        }
        
        .emetteur-box {
            border: 2px solid #0066CC;
            border-radius: 8px;
            padding: 12px 14px;
            background: linear-gradient(135deg, #f8fbff 0%, #ffffff 100%);
        }
        
        .emetteur-name {
            font-size: 11pt;
            font-weight: bold;
            color: #0066CC;
            margin-bottom: 6px;
            border-bottom: 1px solid #e0e0e0;
            padding-bottom: 4px;
        }
        
        .emetteur-details {
            font-size: 8pt;
            color: #333;
            line-height: 1.5;
        }
        
        .emetteur-details strong {
            color: #0066CC;
        }
        
        .client-box {
            padding: 10px 0;
        }
        
        .client-name {
            font-size: 15pt;
            font-weight: bold;
            color: #0066CC;
            margin-bottom: 4px;
            text-transform: uppercase;
        }
        
        .client-details {
            font-size: 9pt;
            color: #444;
            line-height: 1.5;
        }

        /* ==================== OBJECT BANNER ==================== */
        .object-banner {
            background: linear-gradient(135deg, #0088FF 0%, #0055CC 50%, #003399 100%);
            color: #fff;
            text-align: center;
            padding: 14px 20px;
            margin: 15px 0;
            border-radius: 6px;
            box-shadow: 0 3px 10px rgba(0, 102, 204, 0.3);
        }
        
        .object-text {
            font-size: 13pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            text-shadow: 0 1px 2px rgba(0,0,0,0.2);
        }

        /* ==================== TABLE ==================== */
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 5px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }
        
        .items-table thead th {
            background: linear-gradient(180deg, #0077DD 0%, #0055AA 100%);
            color: #fff;
            font-size: 8.5pt;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            padding: 10px 8px;
            text-align: center;
            border: none;
        }
        
        .items-table thead th:first-child {
            text-align: left;
            border-radius: 6px 0 0 0;
            padding-left: 12px;
        }
        
        .items-table thead th:last-child {
            border-radius: 0 6px 0 0;
        }
        
        .items-table tbody td {
            padding: 10px 8px;
            font-size: 9pt;
            border-bottom: 1px solid #e8e8e8;
            vertical-align: middle;
        }
        
        .items-table tbody tr:nth-child(odd) {
            background: #fafbfc;
        }
        
        .items-table tbody tr:nth-child(even) {
            background: #ffffff;
        }
        
        .items-table tbody tr:hover {
            background: #f0f7ff;
        }
        
        .col-designation {
            width: 22%;
            font-weight: 600;
            color: #1a1a1a;
            padding-left: 12px !important;
        }
        
        .col-caracteristiques {
            width: 38%;
            color: #444;
        }
        
        .col-qte {
            width: 8%;
            text-align: center;
            font-weight: 500;
        }
        
        .col-pu {
            width: 16%;
            text-align: right;
            font-family: 'DejaVu Sans Mono', monospace;
            font-size: 8.5pt;
        }
        
        .col-pt {
            width: 16%;
            text-align: right;
            font-weight: 600;
            font-family: 'DejaVu Sans Mono', monospace;
            font-size: 8.5pt;
            color: #0055AA;
        }
        
        /* Total Row */
        .total-row td {
            background: linear-gradient(135deg, #0077DD 0%, #0055AA 100%) !important;
            color: #fff !important;
            font-weight: bold;
            font-size: 11pt;
            padding: 12px 8px;
            border: none;
        }
        
        .total-row td:first-child {
            border-radius: 0 0 0 6px;
        }
        
        .total-row td:last-child {
            border-radius: 0 0 6px 0;
            font-size: 12pt;
        }

        /* ==================== AMOUNT IN WORDS ==================== */
        .amount-section {
            margin: 18px 0;
            padding: 12px 16px;
            background: linear-gradient(135deg, #fffbf0 0%, #fff8e6 100%);
            border-left: 4px solid #FF9900;
            border-radius: 0 6px 6px 0;
        }
        
        .amount-label {
            font-size: 9pt;
            color: #666;
        }
        
        .amount-value {
            font-size: 11pt;
            font-weight: bold;
            font-style: italic;
            color: #CC6600;
            text-decoration: underline;
            text-decoration-color: #FFaa00;
        }

        /* ==================== CONDITIONS ==================== */
        .conditions-section {
            margin: 15px 0;
            padding: 12px 16px;
            background: #f8f9fa;
            border-radius: 6px;
            border: 1px solid #e0e0e0;
        }
        
        .conditions-title {
            font-size: 9pt;
            font-weight: bold;
            color: #CC0000;
            text-decoration: underline;
            margin-bottom: 8px;
        }
        
        .conditions-list {
            font-size: 8.5pt;
            color: #444;
            font-style: italic;
            padding-left: 5px;
        }
        
        .conditions-list p {
            margin: 4px 0;
            padding-left: 12px;
            position: relative;
        }
        
        .conditions-list p:before {
            content: "•";
            position: absolute;
            left: 0;
            color: #CC0000;
            font-weight: bold;
        }

        /* ==================== SIGNATURES ==================== */
        .signatures {
            display: table;
            width: 100%;
            margin-top: 30px;
        }
        
        .sig-client {
            display: table-cell;
            width: 55%;
            vertical-align: top;
        }
        
        .sig-company {
            display: table-cell;
            width: 45%;
            vertical-align: top;
            text-align: center;
        }
        
        .sig-label {
            font-size: 8pt;
            color: #666;
            margin-bottom: 8px;
            font-style: italic;
        }
        
        .sig-box {
            border: 1.5px solid #333;
            border-radius: 4px;
            height: 65px;
            background: #fafafa;
        }
        
        .sig-title {
            font-size: 10pt;
            font-weight: bold;
            color: #1a1a1a;
            margin-bottom: 8px;
        }
        
        .sig-company .sig-box {
            width: 85%;
            margin: 0 auto;
        }

        /* ==================== FOOTER ==================== */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            padding: 8px 10mm;
            background: linear-gradient(180deg, #0066CC 0%, #004499 100%);
            color: #fff;
        }
        
        .footer-company {
            font-size: 8pt;
            font-weight: bold;
            letter-spacing: 0.5px;
        }
        
        .footer-legal {
            font-size: 7pt;
            opacity: 0.85;
            margin-top: 2px;
        }

        /* ==================== WATERMARK FOR DRAFT ==================== */
        {% if proforma.status == 'DRAFT' %}
        body:before {
            content: "BROUILLON";
            position: fixed;
            top: 45%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-45deg);
            font-size: 80pt;
            color: rgba(200, 200, 200, 0.15);
            font-weight: bold;
            z-index: -1;
            pointer-events: none;
        }
        {% endif %}
    </style>
</head>
<body>
    <!-- ==================== HEADER ==================== -->
    <div class="header">
        <div class="header-left">
            <div class="logo-container">
                {% if logo_base64 %}
                    <img src="{{ logo_base64 }}" alt="Logo" class="logo">
                {% else %}
                    <div class="company-text-logo">KTC<span>-Center</span></div>
                {% endif %}
                <div class="slogan">Nouvelle Vision de l'Informatique</div>
            </div>
        </div>
        <div class="header-right">
            <div class="doc-type">Proposition commerciale</div>
            <div class="doc-meta">
                <div class="doc-meta-row">
                    <span class="doc-meta-label">Réf. :</span>
                    <span class="doc-meta-value">({{ proforma.reference }})</span>
                </div>
                <div class="doc-meta-row">
                    <span class="doc-meta-label">Date :</span>
                    <span class="doc-meta-value">{{ proforma.issueDate|date('d/m/Y') }}</span>
                </div>
                <div class="doc-meta-row">
                    <span class="doc-meta-label">Date de fin de validité :</span>
                    <span class="doc-meta-value">{{ proforma.validUntil|date('d/m/Y') }}</span>
                </div>
            </div>
        </div>
    </div>

    <!-- ==================== PARTIES ==================== -->
    <div class="parties">
        <div class="party-emetteur">
            <div class="party-label">Émetteur :</div>
            <div class="emetteur-box">
                <div class="emetteur-name">{{ settings.companyName ?? 'KTC-Center Sarl' }}</div>
                <div class="emetteur-details">
                    {% if settings.address %}{{ settings.address }}<br>{% endif %}
                    {% if settings.postalCode or settings.city %}
                        {% if settings.postalCode %}BP : {{ settings.postalCode }} {% endif %}{{ settings.city ?? 'Yaoundé' }}<br>
                    {% endif %}
                    {% if settings.phone %}
                        <strong>Tél.:</strong> {{ settings.phone }}
                        {% if settings.phone2 %} / {{ settings.phone2 }}{% endif %}<br>
                    {% endif %}
                    {% if settings.email %}<strong>Email:</strong> {{ settings.email }}<br>{% endif %}
                    {% if settings.website %}<strong>Web:</strong> {{ settings.website }}{% endif %}
                </div>
            </div>
        </div>
        <div class="party-spacer"></div>
        <div class="party-client">
            <div class="party-label">Adressé à :</div>
            <div class="client-box">
                <div class="client-name">{{ proforma.client.name }}</div>
                <div class="client-details">
                    {% if proforma.client.address %}{{ proforma.client.address }}<br>{% endif %}
                    {% if proforma.client.city %}{{ proforma.client.city }}{% if proforma.client.country %}, {{ proforma.client.country }}{% endif %}<br>{% endif %}
                    {% if proforma.client.phone %}Tel : {{ proforma.client.phone }}<br>{% endif %}
                    {% if proforma.client.email %}{{ proforma.client.email }}{% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- ==================== OBJECT BANNER ==================== -->
    {% if proforma.object %}
    <div class="object-banner">
        <div class="object-text">{{ proforma.object }}</div>
    </div>
    {% endif %}

    <!-- ==================== ITEMS TABLE ==================== -->
    <table class="items-table">
        <thead>
            <tr>
                <th class="col-designation">Désignation</th>
                <th class="col-caracteristiques">Caractéristiques</th>
                <th class="col-qte">Qtés</th>
                <th class="col-pu">P.U</th>
                <th class="col-pt">P.T</th>
            </tr>
        </thead>
        <tbody>
            {% for item in proforma.items %}
            <tr>
                <td class="col-designation">{{ item.designation }}</td>
                <td class="col-caracteristiques">{{ item.description ?? '' }}</td>
                <td class="col-qte">{{ item.quantityFloat|number_format(0, ',', ' ') }}</td>
                <td class="col-pu">{{ item.unitPriceFloat|number_format(0, ' ', ' ') }} FCFA</td>
                <td class="col-pt">
                    {% if item.totalFloat > 0 %}{{ item.totalFloat|number_format(0, ' ', ' ') }} FCFA{% endif %}
                </td>
            </tr>
            {% endfor %}
            <tr class="total-row">
                <td colspan="4" style="text-align: center; letter-spacing: 2px;">TOTAL</td>
                <td class="col-pt">{{ proforma.totalTTCFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
        </tbody>
    </table>

    <!-- ==================== AMOUNT IN WORDS ==================== -->
    <div class="amount-section">
        <span class="amount-label">Arrêté un montant de : </span>
        <span class="amount-value">{{ proforma.amountInWords ?? (proforma.totalTTCFloat|number_format(0, ' ', ' ') ~ ' FCFA') }}</span>
    </div>

    <!-- ==================== CONDITIONS ==================== -->
    {% if proforma.conditions %}
    <div class="conditions-section">
        <div class="conditions-title">NB :</div>
        <div class="conditions-list">
            {% for line in proforma.conditions|split("\n") %}
                {% if line|trim %}
                <p>{{ line|trim }}</p>
                {% endif %}
            {% endfor %}
        </div>
    </div>
    {% endif %}

    <!-- ==================== SIGNATURES ==================== -->
    <div class="signatures">
        <div class="sig-client">
            <div class="sig-label">Cachet, Date, Signature et mention "Bon pour accord"</div>
            <div class="sig-box"></div>
        </div>
        <div class="sig-company">
            <div class="sig-title">La Direction</div>
            <div class="sig-box"></div>
        </div>
    </div>

    <!-- ==================== FOOTER ==================== -->
    <div class="footer">
        <div class="footer-company">{{ settings.companyName ?? 'Kamer Training and Consulting Center Sarl' }}</div>
        <div class="footer-legal">
            {% if settings.rccm %}N° RCCM: {{ settings.rccm }}{% endif %}
            {% if settings.legalInfo %} &nbsp;•&nbsp; {{ settings.legalInfo }}{% endif %}
        </div>
    </div>
</body>
</html>
