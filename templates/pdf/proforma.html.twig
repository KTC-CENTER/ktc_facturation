<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Proforma {{ proforma.reference }}</title>
    <style>
        @page {
            margin: 15mm 15mm 20mm 15mm;
            size: A4;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'DejaVu Sans', Helvetica, Arial, sans-serif;
            font-size: 10pt;
            color: #333;
            line-height: 1.4;
        }

        /* HEADER */
        .header {
            display: table;
            width: 100%;
            margin-bottom: 15px;
        }
        
        .header-left {
            display: table-cell;
            width: 50%;
            vertical-align: top;
        }
        
        .header-right {
            display: table-cell;
            width: 50%;
            vertical-align: top;
            text-align: right;
        }
        
        .logo { max-height: 55px; max-width: 150px; }
        
        .company-text { font-size: 20pt; font-weight: bold; color: #0066CC; }
        
        .slogan { font-size: 8pt; color: #0066CC; font-style: italic; margin-top: 3px; }
        
        .doc-type { font-size: 18pt; font-weight: bold; color: #C00000; }
        
        .doc-meta { font-size: 9pt; color: #333; margin-top: 5px; line-height: 1.5; }

        /* PARTIES */
        .parties {
            display: table;
            width: 100%;
            margin: 15px 0;
        }
        
        .party-left, .party-right {
            display: table-cell;
            width: 48%;
            vertical-align: top;
        }
        
        .party-spacer { display: table-cell; width: 4%; }
        
        .party-label { font-size: 9pt; color: #666; margin-bottom: 5px; }
        
        .emetteur-box {
            border: 2px solid #0066CC;
            border-radius: 5px;
            padding: 10px 12px;
        }
        
        .emetteur-name { font-size: 11pt; font-weight: bold; color: #000; margin-bottom: 5px; }
        
        .emetteur-details { font-size: 9pt; color: #333; line-height: 1.5; }
        
        .client-name { font-size: 14pt; font-weight: bold; color: #0066CC; margin-top: 10px; }
        
        .client-details { font-size: 10pt; color: #333; margin-top: 5px; line-height: 1.5; }

        /* OBJECT */
        .object-banner {
            background: #0066CC;
            color: #fff;
            text-align: center;
            padding: 10px 15px;
            margin: 15px 0;
        }
        
        .object-text { font-size: 12pt; font-weight: bold; }

        /* TABLE */
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 10px;
        }
        
        .items-table th {
            background: #0066CC;
            color: #fff;
            font-size: 9pt;
            font-weight: bold;
            padding: 8px 6px;
            text-align: center;
            border: 1px solid #0055AA;
        }
        
        .items-table th:first-child { text-align: left; padding-left: 10px; }
        
        .items-table td {
            padding: 8px 6px;
            font-size: 9pt;
            border: 1px solid #ccc;
            vertical-align: top;
        }
        
        .items-table tbody tr:nth-child(even) { background: #f8f8f8; }
        
        .col-designation { width: 22%; font-weight: bold; padding-left: 10px !important; }
        .col-caracteristiques { width: 38%; }
        .col-qte { width: 8%; text-align: center; }
        .col-pu { width: 16%; text-align: right; }
        .col-pt { width: 16%; text-align: right; }
        
        .total-row td {
            background: #0066CC !important;
            color: #fff !important;
            font-weight: bold;
            font-size: 11pt;
            padding: 10px 6px;
        }

        /* AMOUNT */
        .amount-section {
            margin: 15px 0;
            font-size: 10pt;
            font-style: italic;
        }
        
        .amount-value { font-weight: bold; text-decoration: underline; }

        /* CONDITIONS */
        .conditions-section {
            margin: 15px 0;
        }
        
        .conditions-title { font-weight: bold; text-decoration: underline; margin-bottom: 5px; font-size: 9pt; }
        
        .conditions-list { font-size: 9pt; font-style: italic; padding-left: 15px; }
        .conditions-list p { margin: 3px 0; }
        .conditions-list p:before { content: "- "; }

        /* SIGNATURES */
        .signatures { display: table; width: 100%; margin-top: 30px; }
        
        .sig-left { display: table-cell; width: 55%; vertical-align: top; }
        .sig-right { display: table-cell; width: 45%; vertical-align: top; text-align: center; }
        
        .sig-label { font-size: 9pt; margin-bottom: 8px; }
        .sig-title { font-weight: bold; font-size: 10pt; margin-bottom: 8px; }
        
        .sig-box { border: 1px solid #000; height: 65px; }
        .sig-right .sig-box { width: 80%; margin: 0 auto; }

        /* FOOTER */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            padding: 8px 15mm;
            border-top: 2px solid #0066CC;
            font-size: 8pt;
        }
        
        .footer-company { font-weight: bold; color: #0066CC; }
        .footer-legal { font-size: 7pt; color: #666; }
    </style>
</head>
<body>
    <!-- HEADER -->
    <div class="header">
        <div class="header-left">
            {% if logo_base64 %}
                <img src="{{ logo_base64 }}" alt="Logo" class="logo">
            {% else %}
                <div class="company-text">KTC-Center</div>
            {% endif %}
            <div class="slogan">Nouvelle Vision de l'Informatique</div>
        </div>
        <div class="header-right">
            <div class="doc-type">Proposition commerciale</div>
            <div class="doc-meta">
                Réf. : ({{ proforma.reference }})<br>
                Date : {{ proforma.issueDate|date('d/m/Y') }}<br>
                Date de fin de validité : {{ proforma.validUntil|date('d/m/Y') }}
            </div>
        </div>
    </div>

    <!-- PARTIES -->
    <div class="parties">
        <div class="party-left">
            <div class="party-label">Émetteur:</div>
            <div class="emetteur-box">
                <div class="emetteur-name">{{ settings.companyName ?? 'KTC-Center Sarl' }}</div>
                <div class="emetteur-details">
                    {% if settings.address %}{{ settings.address }}<br>{% endif %}
                    {% if settings.postalCode %}BP : {{ settings.postalCode }} {% endif %}{{ settings.city ?? 'Yaoundé' }}<br>
                    {% if settings.phone %}Tél.: {{ settings.phone }}{% if settings.phone2 %} / {{ settings.phone2 }}{% endif %}<br>{% endif %}
                    {% if settings.email %}Email: {{ settings.email }}<br>{% endif %}
                    {% if settings.website %}Web: {{ settings.website }}{% endif %}
                </div>
            </div>
        </div>
        <div class="party-spacer"></div>
        <div class="party-right">
            <div class="party-label">Adressé à :</div>
            <div class="client-name">{{ proforma.client.name }}</div>
            <div class="client-details">
                {% if proforma.client.address %}{{ proforma.client.address }}<br>{% endif %}
                {% if proforma.client.phone %}Tel : {{ proforma.client.phone }}{% endif %}
            </div>
        </div>
    </div>

    <!-- OBJECT -->
    {% if proforma.object %}
    <div class="object-banner">
        <div class="object-text">{{ proforma.object|upper }}</div>
    </div>
    {% endif %}

    <!-- TABLE -->
    <table class="items-table">
        <thead>
            <tr>
                <th class="col-designation">Désignation</th>
                <th class="col-caracteristiques">Caractéristiques</th>
                <th class="col-qte">Qtés</th>
                <th class="col-pu">P.U</th>
                <th class="col-pt">P.T</th>
            </tr>
        </thead>
        <tbody>
            {% for item in proforma.items %}
            <tr>
                <td class="col-designation">{{ item.designation }}</td>
                <td class="col-caracteristiques">{{ item.description ?? '' }}</td>
                <td class="col-qte">{{ item.quantityFloat|number_format(0, ',', ' ') }}</td>
                <td class="col-pu">{{ item.unitPriceFloat|number_format(0, ' ', ' ') }} FCFA</td>
                <td class="col-pt">{% if item.totalFloat > 0 %}{{ item.totalFloat|number_format(0, ' ', ' ') }} FCFA{% endif %}</td>
            </tr>
            {% endfor %}
            <tr class="total-row">
                <td colspan="4" style="text-align: center;">Total</td>
                <td class="col-pt">{{ proforma.totalTTCFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
        </tbody>
    </table>

    <!-- AMOUNT -->
    <div class="amount-section">
        Arrêté un montant de : <span class="amount-value">{{ proforma.amountInWords ?? (proforma.totalTTCFloat|number_format(0, ' ', ' ') ~ ' FCFA') }}</span>
    </div>

    <!-- CONDITIONS -->
    {% if proforma.conditions %}
    <div class="conditions-section">
        <div class="conditions-title">NB :</div>
        <div class="conditions-list">
            {% for line in proforma.conditions|split("\n") %}
                {% if line|trim %}<p>{{ line|trim }}</p>{% endif %}
            {% endfor %}
        </div>
    </div>
    {% endif %}

    <!-- SIGNATURES -->
    <div class="signatures">
        <div class="sig-left">
            <div class="sig-label">Cachet, Date, Signature et mention ''Bon pour accord''</div>
            <div class="sig-box"></div>
        </div>
        <div class="sig-right">
            <div class="sig-title">La Direction</div>
            <div class="sig-box"></div>
        </div>
    </div>

    <!-- FOOTER -->
    <div class="footer">
        <div class="footer-company">{{ settings.companyName ?? 'Kamer Training and Consulting Center Sarl' }}</div>
        <div class="footer-legal">
            {% if settings.rccm %}N° RCCM: {{ settings.rccm }}{% endif %}
            {% if settings.legalInfo %} {{ settings.legalInfo }}{% endif %}
        </div>
    </div>
</body>
</html>
