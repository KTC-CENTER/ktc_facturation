<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Proforma {{ proforma.reference }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        @page {
            margin: 12mm 15mm 20mm 15mm;
            size: A4;
        }
        
        body {
            font-family: 'DejaVu Sans', Arial, sans-serif;
            font-size: 9pt;
            color: #1a1a1a;
            line-height: 1.4;
            background: #fff;
        }
        
        /* Header */
        .header {
            margin-bottom: 20px;
        }
        
        .header-top {
            display: table;
            width: 100%;
            margin-bottom: 15px;
        }
        
        .logo-section {
            display: table-cell;
            width: 25%;
            vertical-align: top;
        }
        
        .logo-section img {
            max-width: 80px;
            max-height: 60px;
        }
        
        .title-section {
            display: table-cell;
            width: 75%;
            vertical-align: middle;
            text-align: right;
        }
        
        .doc-title {
            font-size: 22pt;
            font-weight: bold;
            color: #1E3A5F;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 5px;
        }
        
        .doc-ref {
            font-size: 11pt;
            color: #2E86AB;
            font-weight: 600;
        }
        
        /* Info boxes */
        .info-row {
            display: table;
            width: 100%;
            margin-bottom: 20px;
        }
        
        .info-box {
            display: table-cell;
            width: 48%;
            vertical-align: top;
            padding: 12px 15px;
            background: #f8fafc;
            border-radius: 6px;
        }
        
        .info-box.left {
            border-left: 4px solid #1E3A5F;
        }
        
        .info-box.right {
            border-left: 4px solid #2E86AB;
        }
        
        .info-spacer {
            display: table-cell;
            width: 4%;
        }
        
        .info-title {
            font-size: 8pt;
            text-transform: uppercase;
            color: #6b7280;
            font-weight: 600;
            letter-spacing: 1px;
            margin-bottom: 8px;
            padding-bottom: 5px;
            border-bottom: 1px solid #e5e7eb;
        }
        
        .company-name {
            font-size: 11pt;
            font-weight: bold;
            color: #1E3A5F;
            margin-bottom: 5px;
        }
        
        .info-text {
            font-size: 8.5pt;
            color: #374151;
            line-height: 1.5;
        }
        
        /* Meta info */
        .meta-row {
            display: table;
            width: 100%;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #1E3A5F 0%, #2E86AB 100%);
            border-radius: 6px;
            padding: 12px 20px;
        }
        
        .meta-item {
            display: table-cell;
            text-align: center;
            color: #fff;
        }
        
        .meta-label {
            font-size: 7pt;
            text-transform: uppercase;
            opacity: 0.8;
            letter-spacing: 1px;
        }
        
        .meta-value {
            font-size: 10pt;
            font-weight: bold;
            margin-top: 3px;
        }
        
        /* Object */
        .object-section {
            background: #eff6ff;
            border-left: 4px solid #2E86AB;
            padding: 10px 15px;
            margin-bottom: 20px;
            border-radius: 0 6px 6px 0;
        }
        
        .object-label {
            font-size: 8pt;
            color: #1e40af;
            text-transform: uppercase;
            font-weight: 600;
        }
        
        .object-text {
            font-size: 10pt;
            color: #1E3A5F;
            font-weight: 500;
            margin-top: 3px;
        }
        
        /* Table */
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        
        .items-table thead th {
            background: #1E3A5F;
            color: #fff;
            font-size: 8pt;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            padding: 10px 8px;
            text-align: left;
            border: none;
        }
        
        .items-table thead th:first-child {
            border-radius: 6px 0 0 0;
            width: 5%;
            text-align: center;
        }
        
        .items-table thead th:last-child {
            border-radius: 0 6px 0 0;
            text-align: right;
        }
        
        .items-table thead th.text-right {
            text-align: right;
        }
        
        .items-table thead th.text-center {
            text-align: center;
        }
        
        .items-table tbody tr {
            border-bottom: 1px solid #e5e7eb;
        }
        
        .items-table tbody tr:nth-child(even) {
            background: #f9fafb;
        }
        
        .items-table tbody tr:hover {
            background: #f3f4f6;
        }
        
        .items-table tbody td {
            padding: 10px 8px;
            font-size: 9pt;
            vertical-align: top;
        }
        
        .items-table tbody td:first-child {
            text-align: center;
            color: #6b7280;
            font-weight: 500;
        }
        
        .items-table tbody td:last-child {
            text-align: right;
            font-weight: 600;
        }
        
        .item-designation {
            font-weight: 600;
            color: #1E3A5F;
        }
        
        .item-description {
            font-size: 8pt;
            color: #6b7280;
            margin-top: 3px;
            font-style: italic;
        }
        
        .text-right {
            text-align: right;
        }
        
        .text-center {
            text-align: center;
        }
        
        /* Totals */
        .totals-section {
            display: table;
            width: 100%;
            margin-bottom: 20px;
        }
        
        .totals-spacer {
            display: table-cell;
            width: 55%;
        }
        
        .totals-box {
            display: table-cell;
            width: 45%;
        }
        
        .totals-table {
            width: 100%;
            border-collapse: collapse;
        }
        
        .totals-table tr td {
            padding: 8px 12px;
            font-size: 9pt;
        }
        
        .totals-table tr td:first-child {
            text-align: left;
            color: #6b7280;
        }
        
        .totals-table tr td:last-child {
            text-align: right;
            font-weight: 600;
        }
        
        .totals-table tr.total-row {
            background: linear-gradient(135deg, #1E3A5F 0%, #2E86AB 100%);
            color: #fff;
        }
        
        .totals-table tr.total-row td {
            padding: 12px;
            font-size: 11pt;
            font-weight: bold;
            color: #fff;
        }
        
        .totals-table tr.total-row td:first-child {
            border-radius: 6px 0 0 6px;
            color: #fff;
        }
        
        .totals-table tr.total-row td:last-child {
            border-radius: 0 6px 6px 0;
        }
        
        /* Amount in words */
        .amount-words {
            background: #fef3c7;
            border: 1px solid #fcd34d;
            border-left: 4px solid #f59e0b;
            padding: 12px 15px;
            margin-bottom: 20px;
            border-radius: 0 6px 6px 0;
        }
        
        .amount-words-label {
            font-size: 8pt;
            color: #92400e;
            text-transform: uppercase;
            font-weight: 600;
        }
        
        .amount-words-text {
            font-size: 10pt;
            color: #78350f;
            font-weight: 600;
            font-style: italic;
            margin-top: 3px;
        }
        
        /* Notes */
        .notes-section {
            background: #f0fdf4;
            border: 1px solid #bbf7d0;
            border-left: 4px solid #22c55e;
            padding: 12px 15px;
            margin-bottom: 20px;
            border-radius: 0 6px 6px 0;
        }
        
        .notes-title {
            font-size: 8pt;
            color: #166534;
            text-transform: uppercase;
            font-weight: 600;
            margin-bottom: 5px;
        }
        
        .notes-text {
            font-size: 8.5pt;
            color: #15803d;
            line-height: 1.5;
        }
        
        /* Validity notice */
        .validity-notice {
            text-align: center;
            padding: 10px;
            background: #fef2f2;
            border: 1px solid #fecaca;
            border-radius: 6px;
            margin-bottom: 20px;
        }
        
        .validity-notice.active {
            background: #f0fdf4;
            border-color: #bbf7d0;
        }
        
        .validity-text {
            font-size: 9pt;
            color: #dc2626;
            font-weight: 500;
        }
        
        .validity-notice.active .validity-text {
            color: #16a34a;
        }
        
        /* Signature */
        .signature-section {
            display: table;
            width: 100%;
            margin-top: 30px;
        }
        
        .signature-box {
            display: table-cell;
            width: 48%;
            vertical-align: top;
            padding: 15px;
            border: 1px dashed #d1d5db;
            border-radius: 6px;
            min-height: 80px;
        }
        
        .signature-spacer {
            display: table-cell;
            width: 4%;
        }
        
        .signature-title {
            font-size: 8pt;
            color: #6b7280;
            text-transform: uppercase;
            font-weight: 600;
            text-align: center;
            margin-bottom: 40px;
        }
        
        .signature-line {
            border-top: 1px solid #9ca3af;
            margin-top: 30px;
            padding-top: 5px;
            text-align: center;
            font-size: 7pt;
            color: #9ca3af;
        }
        
        /* Footer */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            padding: 10px 15mm;
            background: #f9fafb;
            border-top: 2px solid #1E3A5F;
            font-size: 7pt;
            color: #6b7280;
            text-align: center;
        }
        
        .footer-company {
            font-weight: 600;
            color: #1E3A5F;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <div class="header-top">
            <div class="logo-section">
                {% if logo_base64 %}
                    <img src="{{ logo_base64 }}" alt="Logo">
                {% endif %}
            </div>
            <div class="title-section">
                <div class="doc-title">Proforma</div>
                <div class="doc-ref">Réf. : {{ proforma.reference }}</div>
            </div>
        </div>
    </div>
    
    <!-- Info boxes -->
    <div class="info-row">
        <div class="info-box left">
            <div class="info-title">Émetteur</div>
            <div class="company-name">{{ settings.companyName ?? 'KTC-Center' }}</div>
            <div class="info-text">
                {% if settings.address %}{{ settings.address }}<br>{% endif %}
                {% if settings.city %}{{ settings.city }}{% if settings.country %}, {{ settings.country }}{% endif %}<br>{% endif %}
                {% if settings.phone %}Tél: {{ settings.phone }}<br>{% endif %}
                {% if settings.email %}Email: {{ settings.email }}<br>{% endif %}
                {% if settings.website %}Web: {{ settings.website }}{% endif %}
            </div>
        </div>
        <div class="info-spacer"></div>
        <div class="info-box right">
            <div class="info-title">Client</div>
            <div class="company-name">{{ proforma.client.name }}</div>
            <div class="info-text">
                {% if proforma.client.address %}{{ proforma.client.address }}<br>{% endif %}
                {% if proforma.client.city %}{{ proforma.client.city }}{% if proforma.client.country %}, {{ proforma.client.country }}{% endif %}<br>{% endif %}
                {% if proforma.client.phone %}Tél: {{ proforma.client.phone }}<br>{% endif %}
                {% if proforma.client.email %}Email: {{ proforma.client.email }}{% endif %}
            </div>
        </div>
    </div>
    
    <!-- Meta info bar -->
    <div class="meta-row">
        <div class="meta-item">
            <div class="meta-label">Date d'émission</div>
            <div class="meta-value">{{ proforma.issueDate|date('d/m/Y') }}</div>
        </div>
        <div class="meta-item">
            <div class="meta-label">Valide jusqu'au</div>
            <div class="meta-value">{{ proforma.validUntil|date('d/m/Y') }}</div>
        </div>
        <div class="meta-item">
            <div class="meta-label">Validité</div>
            <div class="meta-value">{{ proforma.validityDays }} jours</div>
        </div>
    </div>
    
    <!-- Object -->
    {% if proforma.object %}
    <div class="object-section">
        <div class="object-label">Objet</div>
        <div class="object-text">{{ proforma.object }}</div>
    </div>
    {% endif %}
    
    <!-- Items table -->
    <table class="items-table">
        <thead>
            <tr>
                <th>#</th>
                <th>Désignation</th>
                <th class="text-center" style="width: 8%;">Qté</th>
                <th class="text-right" style="width: 15%;">Prix Unit.</th>
                <th class="text-center" style="width: 8%;">Rem.</th>
                <th class="text-right" style="width: 15%;">Total HT</th>
            </tr>
        </thead>
        <tbody>
            {% for item in proforma.items %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>
                    <div class="item-designation">{{ item.designation }}</div>
                    {% if item.description %}
                    <div class="item-description">{{ item.description }}</div>
                    {% endif %}
                </td>
                <td class="text-center">{{ item.quantityFloat|number_format(0, ',', ' ') }}</td>
                <td class="text-right">{{ item.unitPriceFloat|number_format(0, ',', ' ') }}</td>
                <td class="text-center">{% if item.discountFloat > 0 %}{{ item.discountFloat }}%{% else %}-{% endif %}</td>
                <td class="text-right">{{ item.totalFloat|number_format(0, ',', ' ') }} {{ settings.currency ?? 'FCFA' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <!-- Totals -->
    <div class="totals-section">
        <div class="totals-spacer"></div>
        <div class="totals-box">
            <table class="totals-table">
                <tr>
                    <td>Total HT</td>
                    <td>{{ proforma.totalHTFloat|number_format(0, ',', ' ') }} {{ settings.currency ?? 'FCFA' }}</td>
                </tr>
                <tr>
                    <td>TVA ({{ proforma.taxRateFloat }}%)</td>
                    <td>{{ proforma.totalTaxFloat|number_format(0, ',', ' ') }} {{ settings.currency ?? 'FCFA' }}</td>
                </tr>
                <tr class="total-row">
                    <td>TOTAL TTC</td>
                    <td>{{ proforma.totalTTCFloat|number_format(0, ',', ' ') }} {{ settings.currency ?? 'FCFA' }}</td>
                </tr>
            </table>
        </div>
    </div>
    
    <!-- Amount in words -->
    <div class="amount-words">
        <div class="amount-words-label">Arrêté le présent devis à la somme de</div>
        <div class="amount-words-text">{{ proforma.amountInWords ?? (proforma.totalTTCFloat|number_format(0, ',', ' ') ~ ' ' ~ (settings.currency ?? 'FCFA')) }}</div>
    </div>
    
    <!-- Validity notice -->
    <div class="validity-notice {{ proforma.isExpired ? '' : 'active' }}">
        <div class="validity-text">
            {% if proforma.isExpired %}
                ⚠️ Cette proforma a expiré le {{ proforma.validUntil|date('d/m/Y') }}
            {% else %}
                ✓ Proforma valide jusqu'au {{ proforma.validUntil|date('d/m/Y') }}
            {% endif %}
        </div>
    </div>
    
    <!-- Notes / Conditions -->
    {% if proforma.conditions %}
    <div class="notes-section">
        <div class="notes-title">Conditions & Remarques</div>
        <div class="notes-text">{{ proforma.conditions|nl2br }}</div>
    </div>
    {% endif %}
    
    <!-- Signature section -->
    <div class="signature-section">
        <div class="signature-box">
            <div class="signature-title">Cachet & Signature de l'émetteur</div>
            <div class="signature-line">La Direction</div>
        </div>
        <div class="signature-spacer"></div>
        <div class="signature-box">
            <div class="signature-title">Cachet, Date, Signature du client</div>
            <div class="signature-line">Mention "Bon pour accord"</div>
        </div>
    </div>
    
    <!-- Footer -->
    <div class="footer">
        <span class="footer-company">{{ settings.companyName ?? 'KTC-Center' }}</span>
        {% if settings.legalInfo %} | {{ settings.legalInfo }}{% endif %}
        {% if settings.phone %} | Tél: {{ settings.phone }}{% endif %}
        {% if settings.email %} | {{ settings.email }}{% endif %}
    </div>
</body>
</html>
