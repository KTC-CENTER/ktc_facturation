<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Facture {{ invoice.reference }}</title>
    <style>
        @page {
            margin: 10mm 12mm 15mm 12mm;
            size: A4;
        }
        
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            font-family: 'DejaVu Sans', Helvetica, Arial, sans-serif;
            font-size: 10pt;
            color: #333;
            line-height: 1.4;
        }

        /* HEADER */
        .header {
            display: table;
            width: 100%;
            margin-bottom: 12px;
        }
        
        .header-left {
            display: table-cell;
            width: 50%;
            vertical-align: top;
        }
        
        .header-right {
            display: table-cell;
            width: 50%;
            vertical-align: top;
            text-align: right;
        }
        
        .logo { max-height: 55px; max-width: 150px; }
        
        .company-text { font-size: 20pt; font-weight: bold; color: #0066CC; }
        
        .slogan { font-size: 8pt; color: #0066CC; font-style: italic; margin-top: 3px; }
        
        .doc-type { font-size: 18pt; font-weight: bold; color: #C00000; }
        
        .doc-meta { font-size: 9pt; color: #333; margin-top: 5px; line-height: 1.5; }
        
        .status-badge {
            display: inline-block;
            padding: 3px 10px;
            border-radius: 3px;
            font-size: 8pt;
            font-weight: bold;
            margin-top: 5px;
        }
        .status-paid { background: #d4edda; color: #155724; }
        .status-pending { background: #fff3cd; color: #856404; }
        .status-overdue { background: #f8d7da; color: #721c24; }
        .status-partial { background: #d1ecf1; color: #0c5460; }
        .status-cancelled { background: #e2e3e5; color: #383d41; }

        /* PARTIES */
        .parties {
            display: table;
            width: 100%;
            margin: 15px 0;
        }
        
        .party-left, .party-right {
            display: table-cell;
            width: 48%;
            vertical-align: top;
        }
        
        .party-spacer { display: table-cell; width: 4%; }
        
        .party-label { font-size: 9pt; color: #666; margin-bottom: 5px; }
        
        .emetteur-box {
            border: 2px solid #0066CC;
            border-radius: 5px;
            padding: 10px 12px;
        }
        
        .emetteur-name { font-size: 11pt; font-weight: bold; color: #000; margin-bottom: 5px; }
        
        .emetteur-details { font-size: 9pt; color: #333; line-height: 1.5; }
        
        .client-name { font-size: 14pt; font-weight: bold; color: #0066CC; margin-top: 10px; }
        
        .client-details { font-size: 10pt; color: #333; margin-top: 5px; line-height: 1.5; }

        /* OBJECT */
        .object-banner {
            background: #0066CC;
            color: #fff;
            text-align: center;
            padding: 10px 15px;
            margin: 12px 0;
        }
        
        .object-text { font-size: 12pt; font-weight: bold; }

        /* TABLE */
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 8px;
        }
        
        .items-table th {
            background: #0066CC;
            color: #fff;
            font-size: 9pt;
            font-weight: bold;
            padding: 8px 6px;
            text-align: center;
            border: 1px solid #0055AA;
        }
        
        .items-table th:first-child { text-align: left; padding-left: 10px; }
        
        .items-table td {
            padding: 8px 6px;
            font-size: 9pt;
            border: 1px solid #ccc;
            vertical-align: top;
        }
        
        .items-table tbody tr:nth-child(even) { background: #f8f8f8; }
        
        .col-designation { width: 22%; font-weight: bold; padding-left: 10px !important; }
        .col-caracteristiques { width: 38%; }
        .col-qte { width: 8%; text-align: center; }
        .col-pu { width: 16%; text-align: right; }
        .col-pt { width: 16%; text-align: right; }
        
        .subtotal-row td {
            background: #f0f0f0 !important;
            font-weight: bold;
        }
        
        .total-row td {
            background: #0066CC !important;
            color: #fff !important;
            font-weight: bold;
            font-size: 11pt;
            padding: 10px 6px;
        }
        
        .paid-row td {
            background: #d4edda !important;
            color: #155724 !important;
            font-weight: bold;
        }
        
        .due-row td {
            background: #f8d7da !important;
            color: #721c24 !important;
            font-weight: bold;
        }

        /* AMOUNT */
        .amount-section {
            margin: 12px 0;
            font-size: 10pt;
            font-style: italic;
        }
        
        .amount-value { font-weight: bold; text-decoration: underline; }

        /* BANK */
        .bank-section {
            margin: 12px 0;
            padding: 10px;
            background: #f8f9fa;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        
        .bank-title { font-weight: bold; margin-bottom: 5px; font-size: 9pt; }
        .bank-details { font-size: 9pt; }

        /* NOTES */
        .notes-section {
            margin: 12px 0;
        }
        
        .notes-title { font-weight: bold; margin-bottom: 5px; font-size: 9pt; }
        .notes-content { font-size: 9pt; }

        /* FOOTER */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            padding: 8px 12mm;
            border-top: 2px solid #0066CC;
            font-size: 8pt;
        }
        
        .footer-company { font-weight: bold; color: #0066CC; }
        .footer-legal { font-size: 7pt; color: #666; }
    </style>
</head>
<body>
    <!-- HEADER -->
    <div class="header">
        <div class="header-left">
            {% if logo_base64 %}
                <img src="{{ logo_base64 }}" alt="Logo" class="logo">
            {% else %}
                <div class="company-text">KTC-Center</div>
            {% endif %}
            <div class="slogan">Nouvelle Vision de l'Informatique</div>
        </div>
        <div class="header-right">
            <div class="doc-type">FACTURE</div>
            <div class="doc-meta">
                N° : {{ invoice.reference }}<br>
                Date : {{ invoice.issueDate|date('d/m/Y') }}<br>
                Échéance : {{ invoice.dueDate|date('d/m/Y') }}
            </div>
            <span class="status-badge status-{{ invoice.status|lower }}">{{ invoice.statusLabel }}</span>
        </div>
    </div>

    <!-- PARTIES -->
    <div class="parties">
        <div class="party-left">
            <div class="party-label">Émetteur:</div>
            <div class="emetteur-box">
                <div class="emetteur-name">{{ settings.companyName ?? 'KTC-Center Sarl' }}</div>
                <div class="emetteur-details">
                    {% if settings.address %}{{ settings.address }}<br>{% endif %}
                    {% if settings.postalCode %}BP : {{ settings.postalCode }} {% endif %}{{ settings.city ?? 'Yaoundé' }}<br>
                    {% if settings.phone %}Tél.: {{ settings.phone }}{% if settings.phone2 %} /{{ settings.phone2 }}{% endif %}<br>{% endif %}
                    {% if settings.email %}Email: {{ settings.email }}<br>{% endif %}
                    {% if settings.website %}Web: {{ settings.website }}{% endif %}
                </div>
            </div>
        </div>
        <div class="party-spacer"></div>
        <div class="party-right">
            <div class="party-label">Adressé à :</div>
            <div class="client-name">{{ invoice.client.name }}</div>
            <div class="client-details">
                {% if invoice.client.address %}{{ invoice.client.address }}<br>{% endif %}
                {% if invoice.client.phone %}Tel : {{ invoice.client.phone }}{% endif %}
            </div>
        </div>
    </div>

    <!-- OBJECT -->
    {% if invoice.object %}
    <div class="object-banner">
        <div class="object-text">{{ invoice.object|upper }}</div>
    </div>
    {% endif %}

    <!-- TABLE -->
    <table class="items-table">
        <thead>
            <tr>
                <th class="col-designation">Désignation</th>
                <th class="col-caracteristiques">Caractéristiques</th>
                <th class="col-qte">Qtés</th>
                <th class="col-pu">P.U</th>
                <th class="col-pt">P.T</th>
            </tr>
        </thead>
        <tbody>
            {% for item in invoice.items %}
            <tr>
                <td class="col-designation">{{ item.designation }}</td>
                <td class="col-caracteristiques">{{ item.description ?? '' }}</td>
                <td class="col-qte">{{ item.quantityFloat|number_format(0, ',', ' ') }}</td>
                <td class="col-pu">{{ item.unitPriceFloat|number_format(0, ' ', ' ') }} FCFA</td>
                <td class="col-pt">{{ item.totalFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
            {% endfor %}
            
            {% if invoice.taxRateFloat > 0 %}
            <tr class="subtotal-row">
                <td colspan="4" style="text-align: right;">Total HT</td>
                <td class="col-pt">{{ invoice.totalHTFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
            <tr class="subtotal-row">
                <td colspan="4" style="text-align: right;">TVA ({{ invoice.taxRateFloat }}%)</td>
                <td class="col-pt">{{ invoice.totalTaxFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
            {% endif %}
            
            <tr class="total-row">
                <td colspan="4" style="text-align: center;">Total{% if invoice.taxRateFloat > 0 %} TTC{% endif %}</td>
                <td class="col-pt">{{ invoice.totalTTCFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
            
            {% if invoice.amountPaidFloat > 0 %}
            <tr class="paid-row">
                <td colspan="4" style="text-align: right;">Montant payé</td>
                <td class="col-pt">{{ invoice.amountPaidFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
            {% endif %}
            
            {% if invoice.remainingAmountFloat > 0 %}
            <tr class="due-row">
                <td colspan="4" style="text-align: right;">Reste à payer</td>
                <td class="col-pt">{{ invoice.remainingAmountFloat|number_format(0, ' ', ' ') }} FCFA</td>
            </tr>
            {% endif %}
        </tbody>
    </table>

    <!-- AMOUNT -->
    <div class="amount-section">
        Arrêté la présente facture à la somme de : <span class="amount-value">{{ invoice.amountInWords ?? (invoice.totalTTCFloat|number_format(0, ' ', ' ') ~ ' FCFA') }}</span>
    </div>

    <!-- BANK INFO -->
    {% if settings.bankName or settings.bankAccountNumber %}
    <div class="bank-section">
        <div class="bank-title">Coordonnées bancaires :</div>
        <div class="bank-details">
            {% if settings.bankName %}Banque : {{ settings.bankName }}<br>{% endif %}
            {% if settings.bankAccountNumber %}N° Compte : {{ settings.bankAccountNumber }}<br>{% endif %}
            {% if settings.bankIban %}IBAN : {{ settings.bankIban }}{% endif %}
        </div>
    </div>
    {% endif %}

    <!-- NOTES -->
    {% if invoice.notes %}
    <div class="notes-section">
        <div class="notes-title">Remarques :</div>
        <div class="notes-content">{{ invoice.notes|nl2br }}</div>
    </div>
    {% endif %}

    <!-- FOOTER -->
    <div class="footer">
        <div class="footer-company">{{ settings.companyName ?? 'Kamer Training and Consulting Center Sarl' }}</div>
        <div class="footer-legal">
            {% if settings.rccm %}N° RCCM: {{ settings.rccm }}{% endif %}
            {% if settings.legalInfo %} {{ settings.legalInfo }}{% endif %}
        </div>
    </div>
</body>
</html>
